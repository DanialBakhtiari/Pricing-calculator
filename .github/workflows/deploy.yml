name: Build & Publish

# On every push to main: build + test, then publish ONLY the built output
# (dist/) to the `deploy` branch. The host clones/pulls that branch, so it
# serves the built files — never the source.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write # allow pushing to the deploy branch

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build
        env:
          # Must match the sub-path the app is served from on the host.
          VITE_BASE: /pricing/

      - name: Publish dist to the deploy branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: deploy
          folder: dist
          clean: true
          # Append commits (no force-push) so the host can fast-forward
          # `git pull` / cPanel "Update from Remote" without conflicts.
          force: false
          git-config-name: Danial Bakhtiari
          git-config-email: danialbekhtiari@gmail.com
          commit-message: 'deploy build ${{ github.sha }}'
