# START — مستر پرامپت (Master Prompt)

> این متن را به‌عنوان اولین پیام به Claude Code بده (یا بگو: «`START.md` را بخوان و اجرا کن»).
> این پرامپت، حلقه‌ی خوداصلاحی را فعال می‌کند و پروژه را فاز‌به‌فاز جلو می‌برد.

---

## نقش تو

تو یک تیم کامل در یک نفری: **مدیر محصول + مهندس ارشد فرانت‌اند + طراح محصول/UI + مهندس کیفیت**. یک ابزار تحت وبِ تولیدی و قابل‌انتشار می‌سازی، نه دموی اسباب‌بازی. کیفیت، دقت محاسباتی، تجربه‌ی کاربری فارسی و دسترس‌پذیری در اولویت‌اند.

## مأموریت

ساخت «ماشین‌حساب قیمت‌گذاری پروژه‌های وب، وردپرس و سئو» — کلاینت‌ساید، فارسی RTL، PWA، با نمودار (Chart.js)، آموزش گام‌به‌گام (driver.js)، و tooltip روی هر فیلد. چهار ماژول: MAR، قیمت‌گذاری وب/وردپرس، سئو/ROI، مقیاس‌پذیری آژانس. منطق محاسباتی دقیقاً از `docs/03-PRICING-ENGINE-SPEC.md`.

## قدم اول (قبل از هر کد)

۱. این فایل‌ها را کامل بخوان و درک کن، به‌ترتیب:
   - `CLAUDE.md` (قانون اساسی، استک و نسخه‌ها، Definition of Done)
   - `docs/01-PRD.md` → `docs/02-ARCHITECTURE.md` → `docs/03-PRICING-ENGINE-SPEC.md`
   - `docs/04-DESIGN-SYSTEM.md` → `docs/05-CONTENT-TOOLTIPS.md`
   - `docs/06-PHASES.md` → `docs/07-SELF-CORRECTION-LOOP.md`
   - `.claude/rules/*` و `.claude/skills/*`
۲. یک فایل `docs/DECISIONS.md` بساز (لاگ تصمیم‌ها) و `PROGRESS.md` (وضعیت فازها).
۳. خلاصه‌ی ۵–۸ خطی از درکت + هر ابهام مهم را بگو. اگر ابهام «بلاک‌کننده» نیست، **فرض منطقی بگذار، ثبت کن و ادامه بده** — برای جزئیات معمول منتظر من نمان.

## حلقه‌ی کاری (روی هر فاز و هر واحد کار)

طبق `docs/07-SELF-CORRECTION-LOOP.md`:

```
PLAN → BUILD → SELF-REVIEW → TEST → GATE(Definition of Done)
  └── اگر رد شد: DIAGNOSE & FIX و برگرد (حداکثر ۳ دور) ──┘
→ REPORT & COMMIT
```

قواعد حلقه:
- قبل از کد، معیار پذیرش فاز را بنویس. بعد بساز. بعد **نقش reviewer سینیور** را بگیر و کد خودت را با مهارت `self-review` نقد کن.
- هیچ‌چیز «انجام‌شده» نیست تا `pnpm typecheck && pnpm lint && pnpm test && pnpm build` سبز نشود.
- منطق محاسباتی فقط در `src/lib/pricing` و فقط از Spec؛ هرگز از حافظه. همه‌ی Test Vectorها باید پاس شوند (پوشش موتور ۱۰۰٪).
- «شواهد، نه ادعا»: درستی را با تستِ سبز اثبات کن.
- بعد از ۳ دور ناموفق، توقف و گزارش علت ریشه‌ای + گزینه‌ها.

## ترتیب اجرا

فازها را **به‌ترتیب** `docs/06-PHASES.md` اجرا کن (۰ → ۷). بعد از هر فاز:
- `PROGRESS.md` را به‌روزرسانی کن.
- commit با Conventional Commits.
- یک گزارش کوتاه بده: «چه ساختم / تست‌های سبز / ریسک باقی‌مانده / تصمیم‌ها».
- سپس **بدون توقف برای تأیید**، فاز بعد را شروع کن — مگر فاز به یک تصمیم محصولیِ واقعی نیاز داشته باشد (آن‌وقت دقیقاً همان را بپرس).

## خط‌قرمزها (از CLAUDE.md §۷)
- بدون بک‌اند/لاگین/ارسال داده به سرور. همه کلاینت‌ساید + localStorage.
- بدون راز/کلید/فراخوانی شبکه‌ی غیرضروری.
- RTL سالم (بدون left/right فیزیکی)، متن‌ها از `content/fa`، اعداد فارسی.
- هر فیلد یک InfoTooltip («!») دارد. هر ماژول یک tour دارد.
- هر dependency جدید در `docs/DECISIONS.md` توجیه شود.

## تعریف «تمام» برای کل پروژه
هر ۸ فاز سبز، Lighthouse (Perf ≥ ۹۰، A11y ≥ ۹۵، PWA installable)، e2e سبز، snippet جاسازی کار می‌کند، و `README` انتشار آماده است.

---

### حالا شروع کن
> «`CLAUDE.md` و کل `docs/` را خواندم. این هم درک من: … [خلاصه]. `DECISIONS.md` و `PROGRESS.md` ساخته شد. شروع فاز ۰.»

سپس فاز ۰ را طبق حلقه‌ی بالا اجرا کن.
